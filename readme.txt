Spring boot Course

1. Spring Boot 3 Quick Start
2. Spring Core ->  Spring Core is the foundational module of the Spring Framework. It provides the basis for creating and managing objects in an application. One of its key components is the Inversion of Control (IoC) container, which handles the creation and lifecycle of objects. It also includes the Dependency Injection system, making it easy to connect components and manage their dependencies.